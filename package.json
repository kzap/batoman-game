{
  "name": "batoman-game",
  "version": "0.2.0",
  "description": "BatoMan - Filipino cyberpunk 2.5D side-scrolling platformer (Three.js renderer, deterministic 2D sim)",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "npm run typecheck && vite build && npm run budget",
    "preview": "vite preview --port 4173 --strictPort",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tools/tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run --project unit",
    "test:replay": "vitest run --project replay",
    "test:e2e": "playwright test",
    "test:all": "npm run test && npm run test:replay && npm run test:e2e",
    "validate": "tsx tools/validate/index.ts",
    "budget": "tsx tools/validate/budget.ts",
    "ci": "npm run lint && npm run typecheck && npm run test && npm run test:replay && npm run validate && npm run build && npm run test:e2e"
  },
  "dependencies": {
    "postprocessing": "6.39.5",
    "three": "0.186.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.0",
    "@playwright/test": "1.63.0",
    "@types/node": "^22.19.0",
    "@types/three": "0.186.0",
    "eslint": "^9.39.0",
    "sharp": "0.35.4",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "typescript-eslint": "^8.46.0",
    "vite": "^7.0.0",
    "vitest": "5.0.0"
  }
}
